name: Deploy Content to contenthub [17c77a46-07d2-42ba-ad6c-1d5c3b657ad6]
# Note: This workflow will deploy everything in the root directory.
# To deploy content only from a specific path (for example SentinelContent):
#   1. Add the target path to the "paths" property like such
#    paths:
#    - 'SentinelContent/**'
#    - '!.github/workflows/**'
#    - '.github/workflows/sentinel-deploy-17c77a46-07d2-42ba-ad6c-1d5c3b657ad6.yml'
#   2. Append the path to the directory environment variable below
#       directory: '${{ github.workspace }}/SentinelContent'

on:
  push:
    branches: [ test-cd ]
    paths:
    - '**'
    - '!.github/workflows/**'  # this filter prevents other workflow changes from triggering this workflow
    - '.github/workflows/sentinel-deploy-17c77a46-07d2-42ba-ad6c-1d5c3b657ad6.yml'

jobs:
  deploy-content:
    runs-on: windows-latest
    env:
      resourceGroupName: 'michaelva-rg'
      workspaceName: 'contenthub'
      workspaceId: 'ff5b7026-eb37-4ec5-a6cf-a6d2764a95ca'
      directory: '${{ github.workspace }}'
      cloudEnv: 'AzureCloud'
      contentTypes: 'CustomDetection'
      branch: 'test-cd'
      sourceControlId: '17c77a46-07d2-42ba-ad6c-1d5c3b657ad6'
      rootDirectory: '${{ github.workspace }}'
      githubAuthToken: ${{ secrets.GITHUB_TOKEN }}
      smartDeployment: 'true'
    permissions:
      contents: write
      id-token: write # Require write permission to Fetch an OIDC token.

    steps:
    - name: Login to Azure (Attempt 1)
      continue-on-error: true
      id: login1
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_17c77a4607d242baad6c1d5c3b657ad6 }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_17c77a4607d242baad6c1d5c3b657ad6 }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_17c77a4607d242baad6c1d5c3b657ad6 }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 1 failed
      if: ${{ steps.login1.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 2)
      continue-on-error: true
      id: login2
      uses: azure/login@v2
      if: ${{ steps.login1.outcome=='failure' }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_17c77a4607d242baad6c1d5c3b657ad6 }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_17c77a4607d242baad6c1d5c3b657ad6 }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_17c77a4607d242baad6c1d5c3b657ad6 }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 2 failed
      if: ${{ steps.login2.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 3)
      continue-on-error: false
      id: login3
      uses: azure/login@v2
      if: ${{ steps.login2.outcome=='failure'  }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_17c77a4607d242baad6c1d5c3b657ad6 }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_17c77a4607d242baad6c1d5c3b657ad6 }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_17c77a4607d242baad6c1d5c3b657ad6 }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Azure PowerShell script
      uses: azure/powershell@v2
      with:
        azPSVersion: "latest"
        inlineScript: |
          $token = Get-AzAccessToken -AsSecureString | ConvertFrom-SecureString -AsPlainText
          Write-Host $token

    - name: Checkout
      uses: actions/checkout@v3

    - name: Deploy Content to Microsoft Sentinel
      uses: azure/powershell@v2
      with:
        azPSVersion: 'latest'
        inlineScript: |

          ${{ github.workspace }}//.github/workflows/azure-sentinel-deploy-17c77a46-07d2-42ba-ad6c-1d5c3b657ad6.ps1

